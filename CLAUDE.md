# CLAUDE.md

마음쉼 — 불면·불안 완화를 위한 CBT 기반 자가관리 보조 웹앱 (Vite + React + TS, localStorage 전용, PWA, Capacitor).
GitHub push → Netlify 자동배포 → 아이폰 PWA 자동 갱신.

## 디자인 원칙 (화면 레이아웃) — 반드시 지킬 것

새 화면을 만들거나 기존 화면을 수정할 때, 사용자가 매번 "스크롤 없애줘"라고 말하지 않아도
아래 원칙을 **기본값으로** 적용한다.

- **모든 화면은 기본적으로 세로 스크롤 없이 한 화면(뷰포트) 안에 들어가도록 설계한다.**
- 콘텐츠가 화면보다 길어지면 아래 **우선순위 순서로** 조정한다:
  1. 불필요한 여백/마진 축소
  2. 아이콘·이미지 등 장식 요소 크기 축소
  3. 문장을 더 간결하게 축약 (의미는 유지)
- **폰트 크기는 가독성을 위해 임의로 줄이지 않는다.** 이미 정한 폰트 크기 기준을 유지한다.
  (공간이 부족하면 폰트가 아니라 여백·아이콘·문장 길이로 해결한다.)
- **예외적으로 세로 스크롤이 허용되는 화면 = 목록형 화면**: 내용 개수가 가변적인 화면.
  - 예: '배우기' 탭의 세션 목록, '추이' 탭의 기록 리스트.
- 위 예외를 제외한 **모든 화면은 스크롤 없이 한 화면에 담는 것을 기본값으로 한다.**
  - 특히 '기록' 탭의 질문 화면들, '배우기' 탭의 세션 본문 화면들, 소개/요약 화면 등.

### 레이아웃 구현 메모
- 한 화면 = 한 스텝인 플로우는 공통 컴포넌트 `src/components/StepFlow.tsx`를 사용한다.
  (상단 진행바 + n/total, 하단 이전/다음, `position: fixed`로 뷰포트에 맞춰 스크롤 없이 표시)
- StepFlow 내부는 `overflow-x: hidden`으로 가로 넘침을 자체 클리핑한다
  (fixed 컨테이너라 body의 안전장치가 안 먹히므로).
- 시간/텍스트 입력은 `appearance: none` + 클리핑 경계에서 안쪽 인셋(둥근 테두리 잘림 방지).
- 하단 탭바 여백은 `max(8px, calc(env(safe-area-inset-bottom) - 12px))` (홈 인디케이터만 남기고 최소화).
- 한글 줄바꿈은 `word-break: keep-all` (어절 단위), 가로 스크롤 금지.

## 포지셔닝
- "치료"가 아니라 "자가관리 보조 도구". 단, 이 문구를 화면마다 반복·강조하지 않는다.
  필요한 한 줄 안내로만 담는다.

## 검증
- 변경 후 `npm run build` (tsc + vite)로 타입/빌드 확인.
- 커밋·push하면 Netlify가 자동 빌드·배포한다.
